export const jwtConstants = {
    // Palabra secreta que se ocupa para poder verificar que el token que se generó es válido 
    // Este secret nos sirve para verificar que el token que se ha creado
    // coinside con el token que se está recibiendo desde el exterior.
    secret: "no utilizar esta palabra en producción"  
}